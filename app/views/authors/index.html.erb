<h1>All Authors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Homepage</th>
    <th></th>
    </tr>
 
  <% @authors.each do |author| %>
    <tr>
      <td><%= link_to author.name, author_path(author) %></td>
      <td><%= link_to nil, author.homepage %></td>
      <td><%= link_to 'Edit', edit_author_path(author) %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to 'New', new_author_path%>

